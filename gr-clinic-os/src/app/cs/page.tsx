import { ComingSoon } from "@/components/ui";

export default function Page() {
  return (
    <ComingSoon
      title="CS — Reativação e Recorrência"
      responsavel="Ana Julia"
      telas={["Registrar contato/reativação", "Minha carteira por canal (resgate, aniversário, botox…)", "Follow-ups pendentes"]}
    />
  );
}
